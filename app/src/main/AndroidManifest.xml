<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ZeroWasteApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UASPROJECTZEROWASTE">

        <!-- Activity Utama (Launcher/Login) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="false" />
        <activity android:name=".HistoryActivity" android:exported="false" />
        <activity android:name=".FoodListActivity" android:exported="false" />
        <activity android:name=".tampilan_barcode" android:exported="false" android:windowSoftInputMode="adjustResize" />

        <!-- Menyelaraskan nama kelas Java (activity_upload_food / UploadFoodActivity) -->
        <activity android:name=".UploadFoodActivity" android:exported="false" />
        <activity android:name=".activity_upload_food" android:exported="false" />

        <!-- Menyelaraskan nama kelas Java (activity_profile / ProfileActivity) -->
        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".activity_profile" android:exported="false" />

    </application>

</manifest>